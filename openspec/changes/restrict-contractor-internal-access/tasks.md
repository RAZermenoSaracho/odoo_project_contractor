# Tasks

Planning only; all tasks below require explicit apply approval. Database work requires an authorized disposable target; no production changes are implied. Record actual verification results and skips before requesting archive approval.

## 1. Policy inventory and fixtures

- [ ] 1.1 Create the sibling access addon manifest/imports with only the foundation dependency; verify standalone installation in an explicitly authorized disposable database and no automatic user conversion.
- [ ] 1.2 Encode the initial model/operation and RPC/HTTP entry-point policy inventory for the Project dependency closure, including own-account/reference exceptions; verify every reachable model and route is classified and unlisted business surfaces default to denial.
- [ ] 1.3 Build fixtures for two restricted contractors, two customers including same-company colleagues, ordinary employee, managers, two companies and unrelated records; verify baseline upstream broad reads are reproduced before restriction assertions.

## 2. Effective isolation

- [ ] 2.1 Implement protected restricted-user status, minimal groups and guarded user/company/partner/group transitions; verify generic RPC self-promotion, implied-role escalation and unsafe restriction removal fail atomically.
- [ ] 2.2 Implement default-deny model access and conditional AND record-rule ceilings for reviewed models without modifying ordinary-user outcomes; verify CRUD, direct reads, search/count/group/name-search/export on authorized and unrelated records.
- [ ] 2.3 Implement exact resource grant storage and private grant/revoke helpers with company constraints and uniqueness; verify one task grant cannot expose sibling tasks/projects and generic grant mutations are denied.
- [ ] 2.4 Implement reviewed route and callable-method enforcement, including native mail, report and binary surfaces; verify unsupported public methods and elevated controller paths cannot return unrelated records.
- [ ] 2.5 Protect related fields, display names, own-account projections, messages, followers, activities and attachments; verify the indirect-channel scenarios with portal and restricted internal fixtures.
- [ ] 2.6 Implement effective-permission cache invalidation on grants, user type, groups, company and module changes; verify revocation and downgrade through existing sessions and repeated reads without caller cache clearing.
- [ ] 2.7 Implement compatibility checks and fail-closed module/security-change behavior plus uninstall guard for remaining restricted accounts; verify an added broad-ACL test module cannot widen access and unsafe uninstall is refused.

## 3. Verification and handoff

- [ ] 3.1 Run model/route matrix, HTTP/RPC and browser bootstrap tests with the supported module set; record results, denied exceptions and any skips in verification notes, and keep onboarding blocked if a required path fails.
- [ ] 3.2 Run ordinary-employee and standalone-foundation regression tests; record exact Odoo revision, installed modules and test commands/results and validate this change with openspec validate restrict-contractor-internal-access --strict.
