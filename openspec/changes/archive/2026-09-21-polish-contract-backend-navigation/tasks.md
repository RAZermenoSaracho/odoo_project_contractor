# Tasks

## 1. Backend navigation

- [x] 1.1 Add the existing Contracts action as a Project-application menu and verify it does not introduce ACLs or record rules.
- [x] 1.2 Move Project award details into a native Contract notebook tab and add its linked-Contract smart button; verify no duplicate award data is introduced.
- [x] 1.3 Add Contract Proposals/Project smart buttons and Contractor won-Contracts computed count/action, verifying each uses its exact relationship domain.

## 2. Verification

- [x] 2.1 Add focused non-browser ORM tests for every smart-button action and won-Contract count/domain, including unrelated-record exclusion.
- [x] 2.2 Run the focused suite on a disposable database, then strict OpenSpec, archived, and diff validation; sync and archive only this change after evidence.
