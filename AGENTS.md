# Codex Instructions

`CLAUDE.md` is the authoritative source of project-level agent instructions.

Read and follow `CLAUDE.md` before making changes.

Project workflows, commands, skills, and OpenSpec agent resources are maintained under `.claude/`. Use those resources directly rather than creating duplicate `.agents/` copies.
